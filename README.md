# SKkernel

This is System K's kernel and microcode. System K is the kernel space for COODOS,
a Concurrent Object-Oriented Disk Operating System. SKkernel is dependent on
System K's libraries, or SKlib, which holds the C library for the operating system.